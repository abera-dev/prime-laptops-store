# Project rules

## Graphify
- Always run `/graphify .` at the start of every session to build/update the knowledge graph.
- Always save the graph results (graphify-out/ is already present).
- Use `graphify query "<question>"` as the default way to answer codebase questions (architecture, file relationships, data flow, etc.) instead of grepping manually.
